# Журнал выполненных работ

## 2026-09-13 — native Rust чтение и первый simulation

Реализованы строгий AA/55 codec, точная проверка HID descriptor/identity White 1.17, чтение конфигурации и макросов с проверкой адресов, typed DTO/генерация TS, CLI snapshot/colors/ограниченный monitor. Четыре новых теста проверяют 80 сохранённых пакетов, ошибки codec/макросов и ограниченную историю без повторов на каждом ADC-отчёте. Тесты и Clippy прошли.

На устройстве snapshot прочитан; current RGB вернул эхо; за 20 с simulation получено 19 833 пакета, два адреса, максимум 180 мкм. Stop отправлен, конфигурация до/после идентична. Полный ход и физическое соответствие ещё требуют проверки. [Результат](evidence/native-rust-initial.json), [сценарии следующего этапа](EDITOR_IMPLEMENTATION.md).

Контрольная точка: `feat: добавить native Rust чтение IO и ограниченный монитор хода`. UI редакторов и применение ещё в работе.

Записывать только совершённые действия и наблюдаемые результаты. Планируемое находится в PLAN. Для нового результата добавлять дату, объём, проверку, ограничение и ссылку на артефакт/контрольную точку.

## 2026-09-13 — соединение и протокол

**Выполнено:** просмотрены локальные AureTrix и исходное исследование; проверены PnP и HID collections; два vendor HID-интерфейса открыты; статически исследованы HTML и JS официального сайта. Выполнен GET_DEVICE_INFO и затем последовательное чтение блоков настроек временным Python-процессом с hidapi через uv.

**Результат:** IO White `0C45:80D6`, firmware 1.17; рабочий интерфейс FF68:0061, пакеты AA/55. Чтение снимка — 80 пар запрос/ответ с проверкой длины, заголовка, команды, адреса и размера. Проверена карта 84 уникальных физических адресов. Код сайта не исполнялся и в собственный продукт не переносился.

**Ограничение:** SET, сохранение настроек после отключения, analog stream и RGB stream не проверены. Запрос 68 вернул неоднозначные данные. Тестов готового приложения нет, поскольку оно ещё не реализовано. Конфигурация устройства не менялась.

**Артефакты:** [исследование](DEVICE_RESEARCH.md), [протокол](PROTOCOL.md), [снимок](evidence/read-snapshot.json), [метаданные](evidence/device-info.json), [дескрипторы](evidence/hid-descriptors.json), [карта адресов](evidence/layout-reference.json), [источники](evidence/source-manifest.json).

**Контрольная точка:** `0582cb4` — `docs: зафиксировать проверку HID и протокол IO Type 84`.

## 2026-09-13 — архитектура и организация разработки

**Выполнено:** выделены контексты DDD, порты native HID/Windows/storage, аппаратные и программные слои, модель capabilities, сценарий применения/readback/recovery. Подготовлены UX, последовательные фазы, семь самостоятельных описаний модулей и четыре ADR. Подтверждённые пользователем требования перенесены в реестр решений. README приведён к формату публичного репозитория; маршруты чтения описаны для участников и агентов.

**Проверка:** проверены локальные ссылки и явные якоря в 22 Markdown-файлах, валидность пяти JSON-файлов, размеры и SHA-256 десяти блоков, 80 пар пакетов и покрытие всех семи модулей ссылками из плана. Результаты диагностики отделены от неподтверждённых функций. План не помечает реализацию выполненной. Приложение не собиралось и не тестировалось.

**Артефакты:** [архитектура](ARCHITECTURE.md), [план](PLAN.md), [UI](UI.md), [решения](DECISIONS.md), [текущий статус](STATUS.md).

**Контрольная точка:** коммит `docs: спроектировать DDD приложение и правила ведения проекта`.

## 2026-09-13 — прошивка, профиль, LED-панель и Vial

**Выполнено:** по каталогу производителя скачан точный HEX White 1.17, написан самостоятельный анализатор Intel HEX без доступа к устройству. Изучен профиль пользователя. Отдельная LED-панель включена в требования и модель световых поверхностей. Проверена гипотеза Vial по сообщению бренда, исходникам GUI и повторному перечислению HID.

**Проверка:** контрольные суммы всех 5677 HEX-записей корректны, 90 728 байт в двух сегментах; шесть тестов анализатора прошли. Профиль содержит 84 клавиши и 128 RT/RGB-записей, но нулевые RT-пороги нельзя считать текущими аппаратными значениями. Найдены семь HID collections и ноль FF60:0061: штатный Vial GUI отфильтрует текущий IO White. Ролик бренда относится к Custom 68. Настройки устройства не изменялись.

**Ограничения:** MCU/OEM и команды панели не доказаны; пробная Thumb-дизассемблировка — признак архитектуры, не восстановленный исходный код. Vial не запускался, его команды не отправлялись. Отсутствие стандартного Vial-интерфейса не исключает частичное QMK-происхождение firmware. Прямое видео не просмотрено; подтверждение модели взято из публикации бренда.

**Артефакты:** [прошивка](FIRMWARE_RESEARCH.md), [профиль](PROFILE_FORMAT.md), [панель](LED_PANEL.md), [Vial](VIAL_RESEARCH.md), [анализатор](../tools/inspect_firmware.py), [HID enumeration](evidence/vial-compatibility.json).

**Контрольная точка:** коммит `research: разобрать White 1.17, профиль, LED-панель и совместимость Vial`.

## 2026-09-13 — Rust/Tauri/Vue каркас

**Выполнено:** созданы workspace core/platform/cli/desktop, Vue shell с навигацией и разделом LED-панели, типизированный IPC app_info, генерация TS из Rust, lockfiles, локальные иконки и команды разработки/упаковки. Макет переведён на явные координаты скриншота владельца: группы F, правый блок, Space, R-Shift и стрелки. Обновлены README, ADR-0001 и руководство разработчика. HID не добавлялся.

**Проверка:** прошли npm run check (Prettier, Vue/TS, Vite, rustfmt, три Rust-теста, Clippy с -D warnings), CLI help/info и неверная команда с кодом 2. Проверены 84 уникальных адреса относительно fixture, границы геометрии, отсутствие пересечений и выравнивание стрелок. Native release EXE собран. В browser preview до уточнения геометрии отображался UI и работал выбор A; окончательная геометрия проверена численно и сборкой.

**Исправление сборки:** начальный debug EXE при выключенном Vite пытался открыть localhost. Явно включён custom-protocol для packaged build, добавлен регрессионный тест наличия HTML/JS/CSS; он прошёл. Библиотека названа io_desktop_lib для устранения предупреждения Windows о совпадающих PDB.

**Ограничение:** повторный осмотр native-окна и фактический ответ Vue → Rust не подтверждены: управление окном остановлено физическим Escape и не возобновлялось. Тест упаковки не объявляется тестом WebView. NSIS, другие ОС, HID, редакторы, импорт профиля и помощник ещё не проверялись/не реализованы. Настройки клавиатуры не менялись.

**Артефакты:** [каркас и команды](DEVELOPMENT.md), [UI](UI.md), [статус](STATUS.md), `target/release/io-desktop.exe` (локальная сборка, не в Git).

**Контрольная точка:** коммит `feat: собрать каркас Rust Tauri Vue с макетом IO Type 84`.

## 2026-09-13 — AK820, Aether-HE и границы HFD-совместимости

**Выполнено:** разобраны два новых локальных референса, их идентификаторы, codec, размеры таблиц, RT, lighting, TFT, macro и исследовательские материалы. Проверены цепочка fpb → QMK/SonixFlasherC, официальный SN32F299 manual и ARM Cortex-M0 ISA. В коде сайта IO сопоставлены light box, side light, temporary RGB, simulation/FB и macro capacity. Точные локальные ревизии и файлы перечислены в [исследовании](REFERENCE_PROJECTS.md).

**Offline проверка:** собственный harness проверяет SHA-256 референсов, извлекает только рассмотренные чистые функции. Rust `build_frame` AK820 воспроизвёл 80/80 пакетов; Mini60 `build_table_read` — 79/79 обычных GET (68 с key IDs исключён). RT decode → encode совпал для 128 записей; parser Mini60 без отсечения padding дал бы 133. Stacks чужих приложений и Cargo build scripts не запускались. [Отчёт](evidence/reference-codec-comparison.json).

**Устройство:** `tools/probe_aux_lighting.py` сверил точный FF68 descriptor и GET info/identity 1.17, отправил GET `1B` на 24 байта. За 1000 мс ответ не получен. Скрипт остановился без повторов, GET `1D` пропущен, handle закрыт. Два HID Output reports являются запросами чтения; конфигурационных SET не было. [Сырые данные](evidence/aux-lighting-probe.json).

**Новые результаты:** общий framing подтверждён, но приложения не содержат модель IO; Mini60 имеет другие размеры RT/RGB и keymap write. Временный RGB `32` вопреки имени GET отправляет данные. В parser IO `55 FB` байты 2/3 означают keyValue/calibrationStatus, что совпадает с §12 HFD-документа Aether. Промежуточное утверждение о различии этих полей исправлено при финальной сверке. Raw macro capacity=37376, а экспортные 512 могут быть fallback сайта. Дизассемблирование reset IO показывает immediate Thumb-2 BIC/MOV; это не соответствует простой гипотезе Cortex-M0 SN32F299. Точный MCU/OEM и путь прошивки не установлены.

**Проверки:** 10 Python unittest прошли (6 HEX + 4 fail-closed границы probe); offline harness завершился без расхождений. JSON evidence и локальные ссылки новых документов проверены; `git diff --check` чист. Rust/Vue код не менялся; сборка desktop и ручной UI smoke test в этой итерации не повторялись.

**Ограничения:** тайм-аут не доказывает отсутствие команды/панели. SET, simulation, calibration, reset и OTA не запускались; прошивальщики и сторонние приложения не исполнялись. Совместимость framing/RT не доказывает firmware compatibility. Не переносили код референсов в продукт. Отсутствие лицензии у Aether зафиксировано для будущего переноса, анализ и самостоятельная реализация продолжаются.

**Контрольная точка:** коммит `research: проверить совместимость AK820 и Aether с IO White`.
