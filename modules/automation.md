# Модуль: автоматизация

**Ответственность:** детерминированное распознавание событий и условий с выдачей
намерений действий. **Реализация:** `crates/io-core/src/automation.rs`; полный
RuleSet/InputEvent ниже — проект расширения, не существующие типы.

После `objects.md` полная целевая модель — [OBJECT_MODEL](../docs/OBJECT_MODEL.md):
Processor, Recognizer, Condition, State, Flow, arbiters и leases разделены.
Размещение проверяет [execution-plan](execution-plan.md); автономный stock не умеет
принимать весь граф. Детальные контракты ниже остаются проектом до ADR-0009/приёмки.

## Контекст

Это уровень B, работающий при запущенном runtime на штатной firmware. Есть
GestureEngine глубины/удержания 1–8 клавиш и независимых порогов, общий каталог и
профиль v2. FB получен по двум адресам; полный ход и физическая приёмка открыты.
Исполнение включается явно, сейчас зависит от UI polling. Цифровой InputSource,
single/double tap, взаимоисключающие условия и программные слои ещё не реализованы.
Фон — [companion](companion.md), текущие контракты — [action-catalog](action-catalog.md).
Наблюдение и глобальное подавление — разные capabilities.

Для первого расширения правил предпочитаем выделенные назначения и добавочные
действия. Stock DKS/MT/Fn может выдавать цифровой маркер для B —
[кандидат и приёмка](companion.md#stock-trigger). Он передаёт событие, а не полный
ход; не подменять его значением AnalogSample. Выбор физической клавиши и кода
после remap также не тождественны.

У правила явно определяется результат: добавить действие или заменить обычный
ввод. Второе допускается только при подтверждённом stock-маршруте либо отдельной
capability перехвата; observer не получает её из наличия Down/Up. Exclusive tap/deep
или single/double может требовать отложенного решения, а уже выданный ввод не
отменяется. Это требования следующей реализации, не поля существующего profile v2.

## Публичный интерфейс

<a id="exclusive-depth"></a>

### Предложение: исключающий выбор по глубине

2026-09-14 владелец сообщил, что глубокое PgUp вызывает и PgUp, и громкость.
Код согласуется с этим: `GestureEngine::tick` обходит независимые правила, а
`ActionRuntime` добавляет ввод ОС. Общего выбора и владения исходным PgUp нет.
Это незакрытый пользовательский сценарий, не принятая реализация exclusive depth.
Сообщение владельца не заменяет измерения полного FB-потока.

Рекомендуемый первый шаблон для PgUp/PgDn — одно решение на полный цикл:

| Событие | Результат |
|---|---|
| Пересечён лёгкий порог | Запомнить начало, пока ничего не выдавать |
| Отпущено без достижения глубокого порога | Один PageUp/PageDown |
| Достигнут глубокий порог | Volume Up/Down, окончательно отменить лёгкую ветку этого цикла |
| Возврат из глубокой зоны через лёгкую | Не выдавать PageUp/PageDown |
| Полное отпускание | Разрешить новый цикл |
| Потеря данных/сеанса, смена правил | Отменить незавершённое решение, не синтезировать лёгкий tap |

Начальный шаблон однократный. Повтор громкости при глубоком удержании — отдельная
политика с гистерезисом и отменой; shallow autorepeat с последующим исключающим
deep несовместим, если PageUp уже вышел. Для обычного удержания нужен другой шаблон.
Та же причинная граница существует в firmware. Например, QMK задерживает события
до принятия tap/hold-решения: [Tap-Hold](https://docs.qmk.fm/tap_hold#tap-or-hold-decision-modes).

Прежде чем включать шаблон, нужен владелец исходного ввода: подтверждённое штатное
назначение без раннего PgUp, либо отдельный adapter перехвата ОС. Сначала проверить
[stock-мост](companion.md#stock-trigger). Независимый host-автомат поверх по-прежнему
отправляемого PgUp проблему не решает. Windows hook может подавлять сообщения
целевого окна, но не объявляется универсальным захватом всех способов ввода:
[LowLevelKeyboardProc](https://learn.microsoft.com/en-us/windows/win32/winmsg/lowlevelkeyboardproc).

Общая модель задаёт намерение, момент окончательного выбора, конфликтующие ветки
и место исполнения. План применения проверяет доступность источника и владение
выходом; у одного исключающего назначения один владелец. Штатный протокол и будущее
версионированное расширение — adapters единого профиля/редактора. Нельзя молча
заменять exclusive на additive при отсутствии capability или переносить в фон.
Это проект следующего инкремента; существующий profile v2 остаётся без изменений.

Приёмка: лёгкое/глубокое/очень медленное продавливание, возврат через лёгкую зону,
дребезг у порога, повтор, два соседних цикла, обе клавиши вместе, modifiers, сон,
disconnect и остановка помощника. Проверять результат в целевом окне, а не только
срабатывание ActionIntent. Исходную настройку сохранять и восстанавливать обычным
diff; выключение фона не должно скрыто выполнять SET. Ограничение без помощника
нужно объяснить до применения stock-маркера.

### Проект общих типов

| Символ | Тип | Назначение |
|---|---|---|
| RuleSet | aggregate | Версия правил и граф допустимых ссылок |
| Rule | entity | Trigger + Condition + ActionRef + policy |
| SoftwareLayer | entity | Локальный слой с momentary/toggle/приоритетом |
| InputEvent | enum | Down/Up/AnalogSample/ContextChanged/Timer/Disconnected |
| RuleEngine | state machine | Состояние распознавания и детерминированные переходы |
| compile_rules | функция | Проверка неоднозначностей, циклов и возможностей |
| process_event / cancel_session | функции | ActionIntent[] / освобождение runtime-состояния |

## Зависимости

ID устройства/клавиши, Clock, capability requirements и ссылки на каталог Actions. Engine не обращается к HID, Win32, файловой системе и UI. Application включает согласованную версию RuleSet.

## Инварианты

- События одного источника упорядочены и привязаны к поколению сеанса.
- Single/double tap имеет явную политику ожидания; один жест не вызывает оба взаимоисключающих действия случайно.
- Аккорд задаёт одновременность или последовательность, окно времени, отпускание и приоритет конфликтующих правил.
- Autorepeat отличен от повторного физического нажатия. Реинжектированные события не образуют рекурсию.
- Аналоговые пороги имеют гистерезис, единицы и политику устаревания данных. Потеря данных прекращает зависимые действия.
- Смена контекста и RuleSet отменяет или переносит удержание по явно заданной политике; синтетические нажатия имеют учёт владельцев.
- Программные слои не перезаписывают firmware на каждом переключении.

## Намеренно не обрабатывает

Исполнение действий, установку hooks, глобальное подавление клавиш, преобразование хода в силу, гарантии realtime.

## Заметки для реализации

Сначала цифровые триггеры и симулятор. Проверки на виртуальном Clock: границы double tap, отмена при disconnect, два перекрывающихся аккорда, конфликт слоёв, press/release около порога, auto-repeat и остановка при перегрузке. Для аналогового режима отдельный эксперимент должен доказать изменение показаний и их частоту при обычном вводе. [Архитектура runtime](../docs/ARCHITECTURE.md#runtime).
