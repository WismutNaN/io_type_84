{
  "name": "io-type-84",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit",
    "test:editor": "node --experimental-strip-types --test tests/editor-model.test.ts",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 1440",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build --features custom-protocol --no-bundle",
    "desktop:bundle": "tauri build --features custom-protocol",
    "contracts": "cargo run -p io-core --example export_contracts",
    "check": "npm run format:check && npm run build && npm run test:editor && cargo fmt --all --check && cargo test --workspace --features io-desktop/custom-protocol && cargo clippy --workspace --all-targets --features io-desktop/custom-protocol -- -D warnings",
    "format": "prettier --write src vite.config.ts index.html package.json tsconfig.json src-tauri/tauri.conf.json src-tauri/capabilities",
    "format:check": "prettier --check src vite.config.ts index.html package.json tsconfig.json src-tauri/tauri.conf.json src-tauri/capabilities"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "vue": "3.5.42"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "@vitejs/plugin-vue": "6.0.8",
    "prettier": "3.6.2",
    "typescript": "5.9.3",
    "vite": "8.3.0",
    "vue-tsc": "3.3.11"
  }
}
